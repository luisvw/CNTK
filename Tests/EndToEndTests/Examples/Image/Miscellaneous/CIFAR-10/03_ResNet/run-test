#!/bin/bash
. $TEST_ROOT_DIR/run-test-common

# specific CIFAR variables
. $TEST_DIR/../run-test-common

# cntkrun <CNTK config file name> <additional CNTK args>
cntkrun 03_ResNet.cntk "Train=[SGD=[maxEpochs=1]] Train=[SGD=[epochSize=128]] Train=[reader=[randomize=none]] stderr=-"
ExitCode=$?

exit $ExitCode
