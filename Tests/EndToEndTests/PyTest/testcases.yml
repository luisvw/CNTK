dataDir: .

tags:
    # running on windows BVT job in 'E' (Examples) leg
    - bvt-e (os == 'linux')
    # running on windows Nightly job in 'E' (Examples) leg
    - nightly-e (os == 'linux')

testCases:
  PyTest run must finish with error code 0 (outputs __COMPLETED__ in that case):
    patterns:
      - __COMPLETED__

